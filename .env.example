# Optional: set individual provider keys here as an alternative to llm_router_config.json
# The router reads from llm_router_config.json by default.
# These vars are not used by the router directly — add loading logic if needed.

OPENROUTER_API_KEY=sk-or-...
GEMINI_API_KEY=AIza...
GROQ_API_KEY=gsk_...
